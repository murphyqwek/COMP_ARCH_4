### Отчет по Лабораторной работе №4

- **Стариков Арсений Дмитриевич**
- **Группа:** P3213
- **Вариант:** `lisp | cisc | neum | hw | tick | binary | trap | mem | pstr | prob1 | superscalar`

---

## Язык программирования: Lisp-like

Язык представляет собой диалект Lisp с поддержкой глобальных переменных, функций, циклов и обработки прерываний.

### Форма Бэкуса-Наура (БНФ)
```
<program> ::= <expression>*
<expression> ::= <literal> | <symbol> | <list>
<list> ::= "(" (<operator> | <symbol>) <expression>* ")"

<operator> ::= "defvar" | "defun" | "setq" | "if" | "loop" 
             | "print" | "print-pstr" | "print-char" | "in-char" 
             | "+" | "-" | "*" | "/" | "mod" | "adc" | "sbc" 
             | "=" | ">" | "<" 
             | "read-mem" | "write-mem"
             | "progn" | "def-interrupt"

<literal> ::= <integer> | <string>
<symbol> ::= [a-zA-Z_][a-zA-Z0-9_-]*
<integer> ::= [-+]?[0-9]+
<string> ::= '"' [^"]* '"'
```

**Особенности семантики:**
- `defvar`: объявление глобальной переменной.
- `defun`: определение функции с локальными аргументами.
- `setq`: присваивание значения переменной.
- `loop`: цикл вида `(loop <var> <start> <end> <body>)`.
- `def-interrupt`: специальная форма для определения обработчика прерывания (вектор 0x0010).
- **Типы данных:** 32-битные знаковые целые числа и строки (в формате Pascal string: длина + символы).

---

## Организация памяти и регистры

- **Архитектура**: Фон-Неймана;
- **Машинное слово:** 32 бита, знаковое число;
- **Регистры:**
    - `R0, R1, R2, R3`: Регистры общего назначения.
    - `AC1, AC2`: Регистры, которые хранят временный результат вычислений для `ALU1 и ALU2`. Программно они не доступны.
    - `SP`: Указатель стека (используется для `PUSH`, `POP`, `CALL`, `RET` и прерываний).
- **Стек:** Растет вниз, начальное значение `SP = 9000`.

### Память
- Общий объем: 10000 слов.
- **0x0000 - 0x0006:** Зарезервировано: **0x00-0x01 - jmp на секцию кода, 0x02-0x03 - jmp на обработчик прерывания, 0x04-0x06 - MMIO порты**.
- **0x007+:** Секция данных.
- **После секции данных идет обработчик прерывания и секция кода**
- **MMIO порты:**
    - `0x04`: Ввод символа.
    - `0x05`: Вывод целого числа (Int Out).
    - `0x06`: Вывод символа (Char Out).

---

## Система команд (ISA)

Инструкции сериализуются в формат: `[Заголовок] [Аргумент 1] [Аргумент 2] ...`
- **Заголовок (32 бита):**
    - 0-7 биты: Opcode;
    - 8-11 биты: Количество аргументов (0-4);
    - 12-27 биты: Режимы адресации (по 4 бита на аргумент);

### Режимы адресации
1. `IMM` (Immediate): Значение операнда находится в следующем слове;
2. `MEM` (Absolute): Прямой адрес в памяти;
3. `REG` (Register): Работа с регистрами R0-R3, SP;
4. `REG_IND`: Значение в памяти по адресу, хранящемуся в регистре;

### Система команд
| Мнемоника | Опкод | Аргументы | Такты | Описание | Поддерживаемые типы адресации | Пояснение к тактам |
| :--- | :---: | :--- | :---: | :--- | :--- | :--- |
| **NOP** | 0x00 | 0 | **1** | Пропуск такта | — | — |
| **MOV** | 0x01 | 2 (Dist, Src) | **1 – 6** | Копирование значения Src в Dist | Dist: REG, MEM, REG_IND; Src: Все | См. Подробное объяснение тактов команд |
| **ADD** | 0x02 | 3+ (Dist, Args...) | **1 – 15** | Сложение аргументов в Dist | Dist: REG, MEM, REG_IND; Src: Все | См. Подробное объяснение тактов команд |
| **SUB** | 0x03 | 3+ (Dist, Args...) | **1 – 15** | Вычитание аргументов в Dist | Dist: REG, MEM, REG_IND; Src: Все | См. Подробное объяснение тактов команд |
| **MUL** | 0x04 | 3+ (Dist, Args...) | **1 – 15** | Умножение аргументов в Dist | Dist: REG, MEM, REG_IND; Src: Все | См. Подробное объяснение тактов команд |
| **MOD** | 0x05 | 3 (Dist, Arg1, Arg2) | **1 – 9** | Остаток при делении Arg1 и Arg2 и сохранение в Dist | Dist: REG, MEM, REG_IND; Src: Все | См. Подробное объяснение тактов команд |
| **CMP** | 0x06 | 2 (Op1, Op2) | **1 – 6** | Сравнение (обновление флагов) | Все | См. Подробное объяснение тактов команд |
| **JMP** | 0x07 | 1 (Addr) | **1** | Безусловный переход | IMM | — |
| **JEQ** | 0x08 | 1 (Addr) | **1** | Переход, если Z = 1 | IMM | — |
| **JGT** | 0x09 | 1 (Addr) | **1** | Переход, если Z = 0 и N == V | IMM | — |
| **CALL** | 0x0A | 1 (Addr) | **3** | Сохранение PC в стек и переход | IMM | — |
| **RET** | 0x0B | 0 | **3** | Возврат из подпрограммы | — | — |
| **PUSH** | 0x0C | 1 (Src) | **2 – 5** | Запись значения на стек | Все | См. Подробное объяснение тактов команд |
| **POP** | 0x0D | 1 (Dist) | **2 – 5** | Извлечение со стека в Dist | REG, MEM, REG_IND | См. Подробное объяснение тактов команд |
| **IRET** | 0x0E | 0 | **14** | Восстановление регистров и возврат из прерывания | — | — |
| **HALT** | 0x0F | 0 | **1** | Остановка процессора | — | — |
| **ADC** | 0x10 | 3+ (Dist, Args...) | **1 – 15** | Сложение с учетом переноса (C) | Dist: REG, MEM, REG_IND; Src: Все | См. Подробное объяснение тактов команд |
| **SBC** | 0x11 | 3+ (Dist, Args...) | **1 – 15** | Вычитание с учетом заема (C) | Dist: REG, MEM, REG_IND; Src: Все | См. Подробное объяснение тактов команд |
| **Div** | 0x12 | 3 (Dist, Arg1, Arg2) | **1 – 9** | Целочисленнное делениее Arg1 на Arg2 и сохранение в Dist | Dist: REG, MEM, REG_IND; Src: Все | См. Подробное объяснение тактов команд |

### Подробноее объяснеение тактов команд
Для некоторых команд количество тактов зависит от сочетания режимов адресации **Назначения (Dist)** и **Источника (Src)**:

| Режим Dist \ Режим Src | REG | IMM | MEM | REG_INDIRECT |
| :--- | :---: | :---: | :---: | :---: |
| **REG** | 1 | 1 | 2 | 3 |
| **MEM** | 2 | 2 | 4 | 5 |
| **REG_INDIRECT** | 3 | 3 | 5 | 6 |

### Логика для арифметики (ADD, SUB, MUL, ADC, SBC):
Такты вычисляются как сумма стоимости каждого операнда:
1.  **Источники (Args):** `REG/IMM` = +1 такт, `MEM` = +2 такта, `REG_INDIRECT` = +3 такта.
2.  **Назначение (Dist):** `REG` = +0 тактов, `MEM` = +2 такта, `REG_INDIRECT` = +3 такта.
*Пример: `ADD [R0], R1, [100]` (Indirect, Reg, Mem) = 3 (Dist) + 1 (Src1) + 2 (Src2) = **6 тактов**.*
## Микроархитектура и Суперскалярность

Модель процессора имитирует **суперскалярное выполнение**:

1. **Fetch (Выборка):** На каждом шаге Control Unit считывает команду за 1 такт. Потом происходит считывание аргументов (1 аргумент за 1 такт). Затем считывает вторую команду (если первая команда позволяет выполнить две команды одноврееменно).
2. **Анализ зависимостей ():**
   - Проверяются конфликты по данным (RAW, WAW, WAR).
   - Проверяются конфликты по флагам АЛУ.
   - Проверяются конфликты доступа к памяти.
3. **Параллельное исполнение:**
   - Если инструкции независимы, они выполняются за один цикл.
   - Если обнаружена зависимость, выполняется только одна инструкция (но вторая инструкция остаеется в буффере).
---

## Прерывания (Trap)

Система поддерживает аппаратные прерывания по расписанию:
1. При наступлении нужного тика и установленном флаге `IE` (Interrupt Enable), CU сохраняет `PC`, флаги и регистры `R0-R3` в стек.
2. Флаг `IE` сбрасывается.
3. Символ внешнего устройства помещается в ячейку `0x04`.
4. Управление передается по адресу `0x02`.
5. Завершается обработка инструкцией `IRET`.

---

## Транслятор

- Проходит по lisp коду и ищет переменные и строки;
- Аллоцирует переменные и строки в секции данных (с адреса `0x07`);
- Проходится по коду и генерирует машинный код;
- Вывод результата в бинарный файл (.bin) и дизассемблировнный лог (.bin.log).

**Пример запуска:**
```bash
python translator_lisp.py examples/prob1.lisp examples/prob1.bin
```
---
## Модель процессора
Интерфейс командной строки: `python machine.py <code.bin> <schedule.txt>`

Архитектура разделена на **Control Unit** и **Data Path**. Процессор поддерживает потактовое исполнение и аппаратную суперскалярность.

### Схема процессора
![CU](/CISC_CU_SC.png)

![DP](/CISC_DP_SC.png)
---

### Тестирование через Golden Tests:
```bash
pytest .\golden_test.py -v  
```
Тесты проверяют:
1. `cat`: ввод-вывод через прерывания;
2. `hello`: вывод 'Hello World!';
3. `hello_user_name`: запрос имени и его вывод;
4. `double`: арифметика двойной точности;
5. `fact`: вычисление факториала 6 (пример рекурсии);
6. `sort`: алгоритм сортировки в памяти;
7. `test`: скрипт-пример, что любое statement - expression;
8. `prob2`: решение `Euler problem 6`;

### Сравнение производительности

| Алгоритм   | Без суперскалярности (Такты) | С суперскалярностью (Такты) | Прирост скорости |
|------------|------------------------------|-----------------------------|------------------|
| cat        | 550                       | 579                           |  5%           |
| hello | 366                         | 380                        | 11,57%             |
| hello_user_name | 1608                         | 1625                        |1,04%          |
| double | 106                         | 119                        | 10,924%            |
| fact       | 792                         | 851                        | 6,93%             |
| sort       | 3372                         | 3525                        | 4,34%              |
| test       | 570                         | 610                        | 6,55%            |
| prob2      | 9094                      | 21602                     | 7,40%              |

Пример лога с суперсколярным исполнением и прерыванием:
```
    TICK  350 | [INTERRUPT] Accepted sym \0. RetPC set to: 101
    TICK  351 | [TRAP] Char saved
    TICK  351 | SS | PC:  2 | RS:[                    ] | SP:9000 Z:1
    TICK  352 | SS | PC:  2 | RS:[                    ] | SP:8999 Z:1
    TICK  353 | SS | PC:  2 | RS:[                    ] | SP:8998 Z:1
    TICK  354 | SS | PC:  2 | RS:[                    ] | SP:8997 Z:1
    TICK  355 | SS | PC:  2 | RS:[                    ] | SP:8996 Z:1
    TICK  356 | SS | PC:  2 | RS:[                    ] | SP:8995 Z:1
    TICK  357 | SS | PC:  2 | RS:[                    ] | SP:8994 Z:1
    TICK  358 | SS | PC:  2 | RS:[                    ] | SP:8994 Z:1
    TICK  359 | SS | PC:  3 | RS:[                    ] | SP:8994 Z:1
    TICK  360 | SS | PC:  4 | RS:[                    ] | SP:8994 Z:1
    TICK  361 | SS | PC:  5 | RS:[                    ] | SP:8994 Z:1
    TICK  362 | SS | PC:  6 | RS:[JMP:F@2             ] | SP:8994 Z:1
    TICK  363 | SS | PC:  7 | RS:[JMP:F@2             ] | SP:8994 Z:1
    TICK  364 | SS | PC:  8 | RS:[JMP:E@2             ] | SP:8994 Z:1
    TICK  365 | SS | PC: 12 | RS:[                    ] | SP:8994 Z:1
    TICK  366 | SS | PC: 13 | RS:[                    ] | SP:8994 Z:1
    TICK  367 | SS | PC: 14 | RS:[                    ] | SP:8994 Z:1
    TICK  368 | SS | PC: 15 | RS:[                    ] | SP:8994 Z:1
    TICK  369 | SS | PC: 16 | RS:[MOV:F@11            ] | SP:8994 Z:1
    TICK  370 | SS | PC: 17 | RS:[MOV:F@11            ] | SP:8994 Z:1
    TICK  371 | SS | PC: 18 | RS:[MOV:F@11            ] | SP:8994 Z:1
    TICK  372 | SS | PC: 19 | RS:[MOV:E@11            ] | SP:8994 Z:1
    TICK  373 | SS | PC: 20 | RS:[MOV:F@14            ] | SP:8994 Z:1
    TICK  374 | SS | PC: 21 | RS:[MOV:F@14            ] | SP:8994 Z:1
    TICK  375 | SS | PC: 22 | RS:[MOV:F@14            ] | SP:8994 Z:1
    TICK  376 | SS | PC: 23 | RS:[MOV:E@14            ] | SP:8994 Z:1
    TICK  377 | SS | PC: 24 | RS:[                    ] | SP:8994 Z:1
    TICK  378 | SS | PC: 25 | RS:[MOV:F@17,MOV:F@20   ] | SP:8994 Z:1
    TICK  379 | SS | PC: 26 | RS:[MOV:F@17,MOV:F@20   ] | SP:8994 Z:1
    TICK  380 | SS | PC: 27 | RS:[MOV:F@17,MOV:E@20   ] | SP:8994 Z:1
```
